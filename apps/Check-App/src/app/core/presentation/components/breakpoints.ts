// * Cortes de layout, alineados con los breakpoints `sm` (640px) y `lg` (1024px) de Tailwind.
// El corte mobile (< 640px) no vive aquí: lo aplica wrapper.css con una media query para evitar el parpadeo al hidratar.

// Tablet: el sidebar se muestra colapsado a 80px.
export const TABLET_QUERY = '(max-width: 1023.98px)';
